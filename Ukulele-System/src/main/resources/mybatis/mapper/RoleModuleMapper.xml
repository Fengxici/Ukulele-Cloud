<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="timing.ukulele.service.system.mapper.RoleModuleMapper">
    <select id="selectLeafRoleModule" resultType="timing.ukulele.api.model.system.RoleModuleModel">
    SELECT * FROM role_module brm WHERE brm.ROLE_ID = #{roleId} AND
    (SELECT count(0) FROM module_ bms WHERE bms.parent_id = brm.module_id) = 0
  </select>
</mapper>